# Railway deployment configuration - ensures OpenSSL 3.x is available for Prisma
[phases.setup]
nixPkgs = ['nodejs_20', 'openssl_3']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build', 'npx prisma generate']

[start]
cmd = 'npx prisma db push && node dist/index.js'
